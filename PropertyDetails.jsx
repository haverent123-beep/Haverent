import {useEffect,useState} from "react";import {Link,useParams} from "react-router-dom";import Navbar from "../components/Navbar";import Footer from "../components/Footer";import {ArrowLeft,MapPin,MessageCircle} from "lucide-react";import {api} from "../lib/api";
export default function PropertyDetails(){const {id}=useParams();const [property,setProperty]=useState(null);const [error,setError]=useState("");useEffect(()=>{api("/api/properties/"+id).then(d=>setProperty(d.property)).catch(e=>setError(e.message))},[id]);
if(error)return <><Navbar/><main className="details-page"><p>{error}</p></main><Footer/></>;
if(!property)return <><Navbar/><main className="details-page"><p>Loading property...</p></main><Footer/></>;
return <><Navbar/><main className="details-page"><Link to="/properties" className="back-link"><ArrowLeft/>Back to properties</Link><div className="details-image"><img src={property.image||"https://images.unsplash.com/photo-1600607687920-4e2a09cf159d?auto=format&fit=crop&w=1600&q=85"}/></div><div className="details-layout"><div><span className="property-badge static">{property.type}</span><h1>{property.title}</h1><p className="details-location"><MapPin/>{property.location||property.city}</p><h2>About this property</h2><p className="details-description">{property.description||"Comfortable home available for rent."}</p></div><aside className="booking-card"><span>Monthly rent</span><h2>₹{Number(property.rent).toLocaleString("en-IN")}<small>/month</small></h2><button className="primary-full" onClick={()=>alert("Please login to contact/book this property.")}><MessageCircle/>Contact Owner</button></aside></div></main><Footer/></>}
